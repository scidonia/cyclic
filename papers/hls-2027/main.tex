\documentclass[runningheads]{llncs}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}

\usepackage{amsmath,amssymb,mathtools}
\usepackage{array,booktabs,enumitem}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,cd,fit,shapes.geometric}
\usepackage{bussproofs}
\usepackage{listings}
\lstset{basicstyle=\ttfamily\small,breaklines=true,
        keywordstyle=\bfseries,language=Haskell}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}
\usepackage{xcolor}

% Notation macros
\newcommand{\SC}{\textsc{SC}}
\newcommand{\HLS}{\textsc{HLS}}
\newcommand{\ciu}{\approx_{\mathrm{ciu}}}
\newcommand{\jTy}[3]{#1 \vdash #2 : #3}
\newcommand{\tc}{\mathsf{tc}}
\newcommand{\fv}{\mathsf{fv}}
\newcommand{\bind}{\mathbin{>\!\!>=}}
\newcommand{\return}[1]{\mathsf{return}(#1)}
\newcommand{\map}{\mathsf{map}}
\newcommand{\length}{\mathsf{length}}
\newcommand{\sort}{\mathsf{sort}}
\newcommand{\ins}{\mathsf{insert}}
\newcommand{\indep}[2]{\mathsf{indep}(#1,#2)}
\newcommand{\Gen}{\mathsf{Gen}}
\newcommand{\Drop}{\mathsf{dropped}}
\newcommand{\subst}[2]{#1[#2]}
\newcommand{\vmcompute}{\texttt{vm\_compute}}
\newcommand{\keyword}[1]{\textbf{#1}}

\title{Program Transformation as Theorem Proving:\\
       LLMs as Untrusted Proof Oracles}

\author{Anonymous}
\institute{}

\begin{document}
\maketitle

\begin{abstract}
Supercompilation is theorem proving.  Driving is cut elimination, generalisation
is cut introduction, and the trace condition is the global progress check of
cyclic proof theory.  This observation, made precise through mechanisation in
Rocq, leads directly to an architecture where program optimisation is outsourced
to an untrusted oracle and validated by a deterministic proof kernel.

We implement this architecture: an LLM (DeepSeek V4 Pro) proposes cut formulas
(generalisations) and auxiliary lemmas, and the Rocq kernel validates every
proposal through its existing trace-condition check.  Soundness is unconditional
--- the LLM can be replaced by any heuristic without affecting the CIU theorem.
A controlled scrambling experiment shows the LLM succeeds on 6 programs even
with all function and type names randomised, providing evidence of compositional
reasoning from term structure rather than name recognition.

The kernel validates lemmas by running a sub-supercompiler (the same proof
engine) on each proposed statement.  This $\omega$-rule architecture enables
proofs that standard supercompilation cannot achieve: commutativity of addition,
compiler soundness, reverse-append distribution, and parity-filter interaction.
Across 63 primary theorems (81 including infrastructure), 0 are admitted.

A dependent-type extension prunes impossible constructors from type indices,
making programs with different dead branches produce identical residuals ---
a capability absent from all prior supercompilation systems.
\end{abstract}

\input{sections/intro}
\input{sections/background}
\input{sections/sequent-guided-design}
\input{sections/llm-oracle}
\input{sections/examples}
\input{sections/proof-graph}
\input{sections/limits}
\input{sections/evaluation}
\input{sections/related}
\input{sections/conclusion}

\bibliographystyle{splncs04}
\bibliography{refs}

\end{document}
