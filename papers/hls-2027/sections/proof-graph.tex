\subsection{Cyclic proof for reverse-append fusion}
\label{sec:reverse-append-proof}

\begin{figure}[t]
\centering\scriptsize
%% --- TOP: main proof ---
\begin{prooftree}
  \hypo{ys \mapsto \mathsf{nil} \;:\; [ys] \to_s [xs,ys]}
  \infer1[\textsc{back}]{
    \textcolor{red!60}{
      xs,ys \vdash \mathsf{reverse}\;(\mathsf{append}\;xs\;\mathsf{nil}) : \mathsf{List}
      \;\textcolor{red!70}{\dagger}
    }
  }
  \hypo{\textcolor{green!50!black}{\textsc{cons}}_{ys}}
  \infer2[\textsc{iota}]{
    xs,ys \vdash \mathsf{revAcc}\;ys\;(\mathsf{reverse}\;xs) : \mathsf{List}
  }
  \infer1[\textsc{lemma}]{
    xs,ys \vdash \mathsf{revAcc}\;(\mathsf{append}\;xs\;ys)\;\mathsf{nil} : \mathsf{List}
  }
  \infer1[\textsc{fix}]{
    xs,ys \vdash \mathsf{reverse}\;(\mathsf{append}\;xs\;ys) : \mathsf{List}
    \;\textcolor{red!70}{\dagger}
  }
\end{prooftree}

\medskip

\begin{prooftree}
  \hypo{ys' \mapsto \mathsf{nil} \;:\; [ys'] \to_s [xs,ys]}
  \infer1[\textsc{back}]{
    \textcolor{red!60}{
      xs,y{:}\mathsf{Nat},ys \vdash
      \mathsf{reverse}\;xs : \mathsf{List}
      \;\textcolor{red!70}{\dagger}
    }
  }
  \hypo{\text{(recurse on }ys''\text{ — same proof tree)}}
  \infer2[\textsc{iota}]{
    \textcolor{green!50!black}{
      xs,y{:}\mathsf{Nat},ys',ys \vdash
      \mathsf{revAcc}\;ys'\;(\mathsf{cons}\;y\;(\mathsf{reverse}\;xs)) : \mathsf{List}
    }
  }
\end{prooftree}

\medskip

{\footnotesize
\textbf{Fig.~\ref{fig:reverse-append-proof}.}
Cyclic proof for $\mathsf{reverse}\;(\mathsf{append}\;xs\;ys)$.
$\dagger$ = backlink to the root; red = companion.
\textsc{lemma}: LLM cut (proved by sub-prover).
Top: case on $ys$. Nil \textsc{back} with $ys\mapsto\mathsf{nil}$.
Bottom (\textcolor{green!50!black}{green}): identical structure on $ys'$.
The nil \textsc{back} ($ys'\mapsto\mathsf{nil}$) yields $\mathsf{reverse}\;xs$,
which is an instance of the root under $ys\mapsto\mathsf{nil}$ --- the same
backlink target.
The cons branch $\textsc{cons}_{ys'}$ is proved via the cyclic structure:
it is again an instance of $\textsc{cons}_{ys}$ under $ys'\mapsto\mathsf{cons}\;y'\;ys''$.
}

\caption{See caption text above.}
\label{fig:reverse-append-proof}
\end{figure}

Without the \textsc{lemma}, the step from
$\mathsf{revAcc}\;(\mathsf{append}\;xs\;ys)\;\mathsf{nil}$
to $\mathsf{revAcc}\;ys\;(\mathsf{reverse}\;xs)$ is blocked ---
the two terms do not anti-unify, and no fold is possible.
\medskip
