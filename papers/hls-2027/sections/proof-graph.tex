\begin{figure}[t]
\centering
\small
\begin{prooftree}
  \AxiomC{$
    \mathsf{revAcc}\;(\mathsf{append}\;\mathit{xs}\;\mathit{ys})\;\mathit{acc}
    = \mathsf{revAcc}\;\mathit{ys}\;(\mathsf{revAcc}\;\mathit{xs}\;\mathit{acc})
  $}
  \RightLabel{\footnotesize (LLM lemma)}
  \UnaryInfC{$
    \mathsf{revAcc}\;\mathit{ys}\;(\mathsf{reverse}\;\mathit{xs})
    : \mathsf{List}
  $}

  \AxiomC{$\mathsf{ys}\mapsto\mathsf{nil}$}
  \UnaryInfC{$
    \mathsf{reverse}\;\mathit{xs} : \mathsf{List}
  $}

  \BinaryInfC{$
    \mathsf{reverse}\;(\mathsf{append}\;\mathit{xs}\;\mathit{ys})
    : \mathsf{List}
  $}
\end{prooftree}

\vspace{0.3cm}
{\footnotesize The nil branch $\mathsf{reverse}\;\mathit{xs}$ is a substitution instance
of the root goal $\mathsf{reverse}\;(\mathsf{append}\;\mathit{xs}\;\mathit{ys})$
under $\mathit{ys}\mapsto\mathsf{nil}$ --- cyclic backlink closes the proof.}

\caption{
  Cyclic proof for reverse-append fusion.  The LLM-proposed lemma rewrites
  the root; splitting on $\mathit{ys}$ yields nil (closes via cyclic backlink)
  and cons (drives further, folds to root).  Standard SC cannot fuse the two
  sides of the reverse-append identity; the lemma is essential.
}
\label{fig:reverse-append-proof}
\end{figure}
