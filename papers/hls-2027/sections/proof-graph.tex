\begin{figure}[t]
\centering
\resizebox{\textwidth}{!}{%
\small
\begin{tikzpicture}[
  node distance=1.6cm and 1.8cm,
  vtx/.style={rectangle,draw,rounded corners,font=\footnotesize,
              minimum width=3.0cm,minimum height=0.6cm,align=center,inner sep=2pt},
  stuck/.style={vtx,fill=red!10,draw=red!50!black},
  lemma/.style={vtx,fill=green!15,draw=green!50!black},
  driveedge/.style={->,>=stealth,font=\footnotesize},
  foldedge/.style={->,>=stealth,draw=blue!60!black,thick},
  oracle/.style={->,>=stealth,dashed,draw=orange!70!black,font=\footnotesize}
]

\node[vtx] (root) {
  $\mathsf{revAcc}\;(\mathsf{append}\;\mathit{xs}\;\mathit{ys})\;\mathsf{nil}$
};

\node[stuck,below left=of root] (nil) {
  nil: $\mathsf{revAcc}\;\mathit{ys}\;\mathsf{nil}$
};

\node[stuck,below right=of root] (cons) {
  cons: $\mathsf{revAcc}\;\mathit{xs}'\;(\mathsf{cons}\;\mathit{x}\;
   (\mathsf{revAcc}\;\mathit{ys}\;\mathsf{nil}))$
};

\node[lemma,below=of cons] (lemma) {
  \textbf{LLM lemma} \\
  $\mathsf{revAcc}\;(\mathsf{append}\;\mathit{xs}\;\mathit{ys})\;\mathit{acc}
  = \mathsf{revAcc}\;\mathit{ys}\;(\mathsf{revAcc}\;\mathit{xs}\;\mathit{acc})$
};

\node[vtx,below=of lemma] (rewritten) {
  $\mathsf{revAcc}\;\mathit{ys}\;(\mathsf{revAcc}\;\mathit{xs}'\;\mathsf{nil})$
};

\draw[driveedge] (root)  -- node[left]  {split} (nil);
\draw[driveedge] (root)  -- node[right] {split} (cons);
\draw[oracle]    (cons)  -- node[right] {LLM} (lemma);
\draw[driveedge] (lemma) -- node[right] {rewrite} (rewritten);
\draw[foldedge]  (rewritten.east) -- ++(1.0,0) |- (root.east);

\node[draw,dashed,fit=(cons),inner sep=4pt,
      label={[font=\small\bfseries]above right:{stuck}}] {};

\node[draw=none,fill=none,below=of rewritten,yshift=-0.3cm,font=\footnotesize] {
  Standard SC: $r_{\mathsf{std}} \neq r_{\mathsf{std}}'$
  \hspace{1.5cm}
  With lemma: $r_{\mathsf{lemma}} = r_{\mathsf{lemma}}'$
};

\end{tikzpicture}%
}
\caption{
  Cyclic proof graph for reverse-append fusion.  The standard SC gets stuck
  at the cons branch (red); the LLM proposes a lemma (green) that rewrites the
  stuck configuration into a form that folds (blue backlink) to the root.
  Without the lemma, standard SC produces different residuals for the two
  sides of the equation; with the lemma, the residuals are identical.
}
\label{fig:reverse-append-graph}
\end{figure}
